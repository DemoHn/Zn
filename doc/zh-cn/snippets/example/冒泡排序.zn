输入数组文本

如何冒泡排序？
    输入数组
    令排序完成 = 假

    每当排序完成 /= 真：
        注：预先假设当前数组已经排序完成，无需交换
        排序完成 = 真
        以I、K遍历数组：
            注：防止访问数组越界
            如果 I >= 数组之长度：
                结束循环

            如果 数组#{I} > 数组#{I + 1}：
                以数组（交换：I、I + 1）
                排序完成 = 假
    输出数组

注1：文本格式为 123，456，12.4，125 这样子
令分隔文本 = 以数组文本(替换：“，”、“,”)、(分隔：“,”)
令数组 = []
以WB遍历分隔文本：
    以数组(后增：以WB(转换数值))

输出（冒泡排序：数组）