注1：此算法用于演示冒泡排序
如何冒泡排序？
    已知 &数组
    令冒泡 = 真

    每当冒泡：
        冒泡 = 假
        以I、K遍历数组：
            注：if I+1 < arr.length && arr[I] < arr[I+1] 时才交换元素
            如果 I >= 数组之长度：
                （结束循环）

            如果 数组#{I} > 数组#{I + 1}：
                以数组（交换：I、I + 1）
                冒泡 = 真

    返回数组

令X =【1.32、5、-3、8、12、0、-2.3、19】

（显示：（冒泡排序：X））