注1：此算法用于演示冒泡排序
如何冒泡排序？
    已知 &数组
    令冒泡 为真

    每当 冒泡：
        冒泡 为假
        以I、K 遍历数组：
            如果 I之+1 不小于 数组之长度：
                此之（结束）
            注：if I+1 < arr.length && arr[I] < arr[I+1]
            如果 数组#{I} 大于 数组#{I之+1}：                
                （交换元素：数组、I、I之+1）
                冒泡 为真

如何交换元素？
    已知 &数组、I、J
    令T 为数组#{I}
    数组#{I} 为数组#{J}
    数组#{J} 为T

令X为【1.32 5 -3 8 12 0 -2.3 19】

（冒泡排序：X）
（显示：X）